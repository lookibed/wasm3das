# Test fixtures are byte-exact inputs: the Brotli digests in
# wasm3c/test/run-wasi-test.py are computed over the upstream CRLF text, and
# a line-ending conversion silently changes every expected hash.
wasm3c/test/** -text
