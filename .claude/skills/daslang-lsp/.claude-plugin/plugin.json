{
  "name": "daslang-lsp",
  "description": "daslang language server (the daslang checkout pointed to by the DASLANG_ROOT environment variable, at the commit pinned in scripts/daslang_pin) - compiler diagnostics after every edit, plus definition/hover/references/symbols",
  "lspServers": {
    "daslang": {
      "command": "python3",
      "args": [
        "${DASLANG_ROOT}/utils/lsp/lsp_supervisor.py"
      ],
      "env": {
        "DAS_LINT_CONFIG_PATH": "${CLAUDE_PLUGIN_ROOT}/../../../.lint_config"
      },
      "extensionToLanguage": {
        ".das": "daslang"
      },
      "initializationOptions": {
        "compiler": "${DASLANG_ROOT}/bin/daslang",
        "project_root": "."
      }
    }
  }
}
