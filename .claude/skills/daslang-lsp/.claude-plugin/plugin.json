{
  "name": "daslang-lsp",
  "description": "daslang language server from the daScript checkout at /root/daScript - compiler diagnostics after every edit, plus definition/hover/references/symbols",
  "lspServers": {
    "daslang": {
      "command": "/root/daScript/bin/watchdog",
      "args": [
        "--lsp"
      ],
      "env": {
        "DAS_LINT_CONFIG_PATH": "${CLAUDE_PLUGIN_ROOT}/../../../.lint_config"
      },
      "extensionToLanguage": {
        ".das": "daslang"
      },
      "initializationOptions": {
        "project_root": "."
      }
    }
  }
}
